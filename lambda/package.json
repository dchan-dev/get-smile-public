{
  "name": "get-smile",
  "version": "1.0.0",
  "description": "Lambda function to retrieve product information from S3 and display as HTML",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.400.0",
    "@aws-sdk/client-sqs": "^3.787.0",
    "csv-parser": "^3.0.0",
    "ioredis": "^5.6.1"
  }
}
